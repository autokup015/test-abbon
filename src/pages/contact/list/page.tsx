import type { FC } from "react";

const metadata = { title: `Contact List` };

// ---------------------------------------------------------------------------------

const Page: FC = () => {
  return (
    <>
      <title>{metadata.title}</title>
      
      Page
    </>
  );
};

export default Page;
